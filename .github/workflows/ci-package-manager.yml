name: ci-package-manager

on:
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

jobs:
    ci-package-manager:
        uses: eslint/workflows/.github/workflows/ci-package-manager.yml@45216eddf520165b268c91f3c55a91eb6cec19dd # main
