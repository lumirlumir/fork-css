name: ci-bun

on:
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

jobs:
    ci-bun:
        uses: eslint/workflows/.github/workflows/ci-bun.yml@45216eddf520165b268c91f3c55a91eb6cec19dd # main
