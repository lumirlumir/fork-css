name: update-readme

on:
    schedule:
        - cron: "0 8 * * *" # Runs every day at 08:00 AM UTC

    workflow_dispatch:

jobs:
    update-readme:
        uses: eslint/workflows/.github/workflows/update-readme.yml@d533003faa62d8d06f2fef67cb5afbe75d643dfd # main
        secrets:
            workflow_push_bot_token: ${{ secrets.WORKFLOW_PUSH_BOT_TOKEN }}
