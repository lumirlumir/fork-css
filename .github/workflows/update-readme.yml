name: update-readme

on:
    schedule:
        - cron: "0 8 * * *" # Runs every day at 08:00 AM UTC

    workflow_dispatch:

jobs:
    update-readme:
        uses: eslint/workflows/.github/workflows/update-readme.yml@45216eddf520165b268c91f3c55a91eb6cec19dd # main
        secrets:
            workflow_push_bot_token: ${{ secrets.WORKFLOW_PUSH_BOT_TOKEN }}
