name: ci-package-manager

on:
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

jobs:
    ci-package-manager:
        uses: eslint/workflows/.github/workflows/ci-package-manager.yml@d533003faa62d8d06f2fef67cb5afbe75d643dfd # main
