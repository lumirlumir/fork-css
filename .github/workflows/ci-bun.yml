name: ci-bun

on:
    push:
        branches:
            - main

    pull_request:
        branches:
            - main

jobs:
    ci-bun:
        uses: eslint/workflows/.github/workflows/ci-bun.yml@d533003faa62d8d06f2fef67cb5afbe75d643dfd # main
